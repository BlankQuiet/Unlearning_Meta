# Verified working versions (this exact environment ran every experiment
# in ARCHITECTURE.md §10-18, including the n=55 pre-registered result).
# Loose lower bounds would likely work too, but these are the versions
# actually tested, not a guess.
torch>=2.0.0
torchvision>=0.15.0
numpy>=1.24.0
matplotlib>=3.7.0
scikit-learn>=1.3.0
scipy>=1.10.0
